# 依赖库名称
vue-hk-video-test

**依赖库设计**
 把前端代码部署到服务器，通过Iframe访问

**主要特性**

- 查询海康的监控数据。

## 使用说明

### 组件（组件名）

- **methods**
  - methodName(params): 通过Iframe组件访问，入参为url必填

### 逻辑（逻辑名）

- **methodName(params):** 描述方法的作用和用法和返回值类型，传入参数的类型和参数是否为必填
